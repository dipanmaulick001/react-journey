# 🪝 useFetch – React Custom Hook (Dynamic URL based fetching)

This project demonstrates a custom React Hook (`useFetch`) that automatically fetches data from an API and re-fetches whenever the URL changes.

## ⭐ Features
- Fetch JSON data using `fetch`
- Saves response in React state
- Fetches automatically on mount
- Re-fetches whenever URL changes
- Clean + reusable logic

---

## 📁 Project Structure

src/
├── App.jsx
├── hooks/
│ └── useFetch.js


